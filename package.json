{
  "name": "service-worker",
  "version": "1.0.0",
  "description": "Creating my first service worker",
  "scripts": {
    "start": "http-server ./public -p 3020",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniel Austin",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.11.1"
  }
}
